# Dependencies
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Secrets (Force usage of Docker Compose env vars)
.env
.env.local

# Git & System
.git
.DS_Store
.vscode
.idea

# Data Folders (prevent baking huge files into the image)
# We rely on docker volumes to mount these at runtime instead
server/videos
server/results
server/thumbnails